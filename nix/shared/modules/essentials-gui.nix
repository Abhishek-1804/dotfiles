{ pkgs, ... }:

[
  # Essential GUI Applications
  # Add basic GUI apps that are commonly needed across platforms
  # Platform-specific GUI apps (like Homebrew casks on macOS) should remain in platform configs
]
